# --- core scientific stack ---
numpy>=2.0,<3
scipy>=1.14
xarray>=2024.6
cf-xarray>=0.9
h5netcdf>=1.3
netCDF4>=1.7
scikit-image>=0.24
pandas>=2.2

# --- api ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-multipart>=0.0.9

# --- raster / encoding ---
pillow>=10.4
matplotlib>=3.9

# --- config / cli ---
pyyaml>=6.0
typer>=0.12

# --- standards: OGC WMS + OPeNDAP (fail-soft, see api/main.py) ---
xpublish>=0.5
xpublish-wms>=0.13
xpublish-opendap>=0.2

# xpublish-opendap -> opendap-protocol still imports pkg_resources, which
# setuptools removed in 81. Pin below that. (Verified: 84 fails, 80.10.2 works.)
setuptools<81
